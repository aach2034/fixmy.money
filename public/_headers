# Keep every static asset revalidating unless the application Worker verifies
# the exact content fingerprint and overrides this safe default.
/assets/*
  Cache-Control: public, max-age=0, must-revalidate
