interface ChatbotWidgetProps {
  context?: string;
}

/** The generic AI assistant is disabled during Phase 0 containment. */
export default function ChatbotWidget(_props: ChatbotWidgetProps) {
  return null;
}
