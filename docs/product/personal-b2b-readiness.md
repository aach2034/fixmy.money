# Personal/B2B readiness record

Status: **HOLD — not cleared for new paid transactions or launch.** Updated 2026-09-22 16:00 UTC. This is an engineering and billing evidence record, not legal or tax advice. It distinguishes the merged GitHub branch from the separately deployed Sites source.

## Current production and rollback

- GitHub [PR #27](https://github.com/aach2034/fixmy.money/pull/27), “Fix stale trial label for active subscriptions,” merged into `main` at 2026-09-22 15:53:52 UTC, merge commit `a7ac510f84945f18f3745d8ba2ae193eb05544d2`. Its diff is exactly three files: `src/lib/subscription/display.ts`, `src/app/billing-subscriptions/components/BillingContent.tsx`, and `src/__tests__/subscription-display.test.ts` (96 additions, 2 deletions). The helper shows “Trial ends” only when the server-returned entitlement state is `trial`, server-verified Stripe status is `trialing`, and the date is valid and future. It does not change entitlement calculation or Checkout.
- Sites production version **199**, saved version ID `appgprj_6a5ad9918cc48191a7f98216d16add39~appgver_96d36de8988081918d43cfbbc2a2ced1`, deployment `appgdep_6ab2a54cd7748191a31423b6825e0b29`, succeeded at [fixmy.money](https://fixmy.money). Sites source commit `ab9de4cdf16dd1dc70ceeaea62c55d71c502820e` applies exactly the same three-file PR #27 patch to the previously deployed v198 source. This preserves v198's universal Checkout guard and avoids shipping unrelated saved v197 changes. The Sites source commit is **not** the GitHub `main` merge commit.
- Exact rollback target: Sites **v198**, saved version ID `appgprj_6a5ad9918cc48191a7f98216d16add39~appgver_59cecab8416c81918b80f0351dfe4f6c`, deployed previously as `appgdep_6ab24ab1aebc8191bbdd130b05814662`, source `99514a1da37c968bf3f49b8cebd8dadca16fd067`. It retains the universal Checkout hold but restores the stale trial-label defect. Do not roll back to v196: its eligible-existing-owner Checkout path was open.
- Post-v199 production `GET /api/health` returned 200. An unauthenticated, non-chargeable `POST /api/stripe/create-checkout` with `starter` returned HTTP 503, `PAID_CHECKOUT_ON_HOLD`, with no Checkout URL. The live Checkout Session list contained **20**, `has_more=false`, before deployment validation and after the browser and direct-route checks. No new Session was observed.
- Control boundary: **new Checkout Session creation is blocked**. This guard does **not** stop renewals on existing subscriptions or payment/collection of existing invoices. The open historical $129 invoice `in_1Tzib66scmm1R60B8GVdO0AB` was not altered, collected, canceled, or forgiven here.

## Authenticated production validation

A signed-in Safari session was available and used without entering or resetting credentials. At 2026-09-22 16:00 UTC, Billing Center loaded, but showed `Free Plan`, `No Plan Selected`, `Expired`, and `Could not verify your subscription.` Clicking dashboard navigation redirected to `/billing-subscriptions?reason=subscription_check_failed`. The required “Starter Active,” “Manage my subscription,” dashboard, and ordinary account-access checks **did not pass** in this session. This same failure was observed in the prior production check before v199, so the observation alone does not establish that PR #27 caused it. The PR description reports that a separate authorized existing-customer check passed earlier; that claim was not reproduced by this post-deployment session. No Checkout, portal, payment, or plan-selection action was clicked. Identity and authentication data are deliberately omitted. This was a focused manual browser check, **not** an automated Playwright test. Production access remains a verification gate.

## Tests and scope

- PR #27's required GitHub workflow run `35723508016` succeeded: quality, integration, migration replay, Firefox, Chromium, WebKit, mobile WebKit, and release-gate jobs all concluded successfully. The PR author also recorded a local Playwright Chromium-download failure; it does not negate the successful CI browser jobs, and no local Playwright run is claimed here.
- On the v198-based Sites release tree, focused display/Checkout tests passed 14/14; full unit suite passed 1,363/1,363; TypeScript passed; changed-file lint passed; Vinext production build passed. The release-to-v198 source diff is only the three PR #27 files. A `pnpm build` wrapper attempt failed while trying to fetch pnpm from the blocked registry; invoking the already-installed Node/Vinext build directly succeeded. No application runtime setting was changed.
- No Stripe catalog, customer, subscription, invoice, payment method, charge, payment link, tax setting, Supabase object, or enrollment state was intentionally changed by this display release. The 20-to-20 Checkout Session check is evidence only for Sessions; it is not a full before/after audit of every Stripe object.

## Live Stripe catalog — read-only at 2026-09-22 16:00 UTC

All six listed USD monthly prices are active; `has_more=false` for the live Product and Price lists. Intended prices have the lookup keys shown. **No pointer or price change has been made.**

| Product | Product ID | Current default (legacy) | Intended active default | Lookup key |
|---|---|---|---|---|
| Personal | `prod_UuFTIQu4WZ47UI` | $49 `price_1TuQyA6scmm1R60Bn9f3UP7h` | $39 `price_1U0pLH6scmm1R60BUsEZcBvm` | `fixmy_personal_monthly` |
| Start | `prod_UuFTYxCGOhMZCS` | $129 `price_1TuQyY6scmm1R60BNdVbBlp8` | $99 `price_1U0pP06scmm1R60Bvez6KnmW` | `fixmy_start_monthly` |
| Grow | `prod_UuFToC81TfFJav` | $249 `price_1TuQym6scmm1R60BrjvJQaFC` | $199 `price_1U0pQW6scmm1R60B862U1Ea7` | `fixmy_grow_monthly` |

The live Personal Product description still says “AI-assisted credit repair software for your own profile and up to 3 friends or family members,” contrary to the intended **three profiles total, including the subscriber**. Correcting that description is a separate, still-pending Product write, not part of PR #27.

Stripe [documents](https://docs.stripe.com/api/products/update) that a Product update changes only supplied fields; its [catalog guide](https://docs.stripe.com/products-prices/manage-prices) shows an existing active Price can become `default_price`. Therefore a request supplying only `default_price` on these three Products does not itself update a subscription item, archive the old Price, or create an invoice, charge, Checkout Session, or payment link. This is API-behavior reasoning, **not** a substitute for live post-write readback. Flows that dynamically use a Product default would see the new default; the universal Checkout hold must stay active. Existing customer migrations are not authorized. The open $129 invoice and its historical Price must remain intact.

The connected Stripe write permission was reported in PR #27 as lacking `product_write`; it was not broadened or tested with a live write in this task. The least-privileged connector change is **Products: Write** for this live account, retaining only already-needed read scope; no unrestricted secret key or Customer, Subscription, Invoice, or Payment write permission is needed for the three pointer updates. [Stripe restricted-key guidance](https://docs.stripe.com/keys/restricted-api-keys) likewise favors resource-scoped Write over an unrestricted key. Never paste a live key into chat, source, logs, or this record. No Product update should be attempted until the owner completes the permission step and an authorized change window is reconfirmed.

## Tax and remaining gates

Live Stripe Tax settings were read-only checked: `status=pending`, `head_office=null`, default tax behavior `exclusive`, default code `txcd_10103001`. Prior reconciliation found no registrations. A qualified tax professional must decide taxability and registrations before new paid transactions; the owner accepted pending Tax only for catalog-only cleanup, **not** reopening sales.

Remaining gates: reproduce existing-customer Starter/billing/dashboard access; correct and read back the three Product defaults and Personal description with appropriately scoped authorization; preserve historical prices and the open invoice; reconcile any existing-customer transition separately; resolve Stripe Tax; complete launch-state, disclosure/consent, claims, privacy/retention, and production security review. The owner's 2026-09-22 exception treats missing copies of counsel-approved documents as a documentation follow-up for this narrow reconciliation, not as an unresolved counsel-approval blocker. No conclusion here authorizes signup, paid Checkout, enrollment, migration, invoice collection, or launch.
